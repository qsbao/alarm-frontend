/**
 * alarmStore — UI-only state for the alarm queue.
 *
 * All alarm data and mutations now live on the backend.
 * Alarm filtering/pagination uses URL params (see AlarmListPage).
 * This file is intentionally minimal.
 */
